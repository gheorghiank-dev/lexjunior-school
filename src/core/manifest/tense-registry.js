/**
 * Legacy file kept temporarily to avoid dead imports during migration.
 *
 * Runtime consumers should import from src/modules/tenses/registry.js.
 * This file intentionally has no imports so src/core remains independent
 * from src/modules.
 */

export {};
